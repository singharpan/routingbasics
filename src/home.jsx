import React from "react";

const Home = () => {
  return (
    <React.Fragment>
      <h2>This is home page</h2>
      <sectio>
        JavaScript is a single-threaded programming language which means only
        one thing can happen at a time. That is, the JavaScript engine can only
        process one statement at a time in a single thread.
      </sectio>
      <p>So without further ado, Let’s get started :)</p>
    </React.Fragment>
  );
};

export default Home;
